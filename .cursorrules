Act as an expert Expert Frontend Developer. I want to redesign the UI and layout of my authentication pages (Login & Register) to closely match the design of the provided screenshot/reference from `https://mumoira.tv`.

Here are the CRITICAL RULES you must follow:
1. STRICTLY PRESERVE ALL LOGIC: Do not touch, alter, or remove any existing business logic, state management (e.g., useState, hooks), API calls, validation logic, routing, or event handlers (onSubmit, onChange, onClick).
2. ONLY UPDATE UI/UX: Your job is purely to change the HTML/JSX structure and the styling (classes/CSS) to match the target design.
3. MATCH THE TARGET DESIGN: Apply the visual style of mumoira.tv based on my description and attached images. This includes:
   - Dark mode background and specific color palettes.
   - Layout structure (e.g., centered modal or split screen).
   - Input field styles, typography, buttons, and hover/focus effects.
4. TECH STACK: Use [Điền framework CSS của bạn vào đây, ví dụ: Tailwind CSS / SCSS / Bootstrap] for styling.
5. RESPONSIVE: Ensure the new UI looks good on both mobile and desktop.

Instructions:
- Review the code in the files I have tagged.
- Identify the logic layer and separate it from the UI layer mentally.
- Rewrite the render/return portion with the new beautiful UI while mapping the exact same state variables and functions to the new inputs and buttons.
- Do not generate dummy logic; use what is already there.

Please output the updated code.